package ru.sinara;


public class TestLoggingImpl implements TestLogging {
    private static final Loggerrrrr log = LoggerFactory.getLogger(CalcDemo.class);

    @Override
    public void calculation(int param) {
        log.
    }
}
